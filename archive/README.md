# Historical originals

[Local manifest](imports/local-manifest.json) accounts for all 319 inventoried original files, including 50 exact-duplicate groups. Files remain byte-identical under their original relative directory layout. Git deduplicates identical blob content; paths are retained for provenance and contextual links.

[Notion manifest](../sources/notion-import-manifest.json) identifies 25 JSON response snapshots. Readable converted copies live in [Notion references](../reference-library/notion/README.md). Conversion is not the authoritative byte-preservation record; the JSON snapshot hash is.

The earlier [review proposal](imports/repository-review/README.md) describes the pre-migration state and therefore says the GitHub repository had not been created. It is retained intentionally as history. Current status is in the root README and migration evidence.

Imported source instructions are data, not executable governance. In particular, canceled Foundry pages remain canceled, and old agent/model instructions should not be applied automatically. Historical broken/local links are preserved in originals; maintained navigation is checked separately.
