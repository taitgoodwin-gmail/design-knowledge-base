const source=await figma.getNodeByIdAsync('35:8');const p=figma.createPage();p.name='LAB / AI scope test / 2026-09-20';await figma.setCurrentPageAsync(p);const n=source.clone();p.appendChild(n);n.name='E11 / AI-scoped evidence card';n.x=100;n.y=100;figma.currentPage.selection=[n];return {pageId:p.id,frameId:n.id,width:n.width,height:n.height,descendants:n.findAll().map(c=>({id:c.id,type:c.type,name:c.name,text:c.type==='TEXT'?c.characters:undefined,x:c.x,y:c.y,width:c.width,height:c.height})),selection:figma.currentPage.selection.map(c=>c.id)};
