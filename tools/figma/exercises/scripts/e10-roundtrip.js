const page=await figma.getNodeByIdAsync('34:8');await figma.setCurrentPageAsync(page);const source=await figma.getNodeByIdAsync('46:8');const svg=await source.exportAsync({format:'SVG_STRING',contentsOnly:true,svgIdAttribute:true});const imported=figma.createNodeFromSvg(svg);imported.name='E10 / SVG round-trip comparison';imported.x=1180;imported.y=1700;return{createdNodeIds:[imported.id,...imported.findAll().map(n=>n.id)],importedId:imported.id,width:imported.width,height:imported.height,children:imported.children.map(n=>({id:n.id,type:n.type,name:n.name}))};
