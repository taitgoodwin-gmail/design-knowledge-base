const page=await figma.getNodeByIdAsync('34:8');await figma.setCurrentPageAsync(page);const grid=await figma.getNodeByIdAsync('37:12');await figma.loadFontAsync({family:'Inter',style:'Regular'});const results=[];for(const c of grid.children){try{c.layoutSizingHorizontal='FILL';results.push({id:c.id,result:'accepted',width:c.width});}catch(e){results.push({id:c.id,result:'rejected',message:String(e)});}}return{mutatedNodeIds:grid.children.map(c=>c.id),results,tracks:grid.gridColumnSizes.map(t=>({type:t.type,value:t.value}))};
